import test from'node:test';import assert from'node:assert/strict';import{dailyTotals,mealNutrition,resolvedKcal,targetForDate}from'../js/calculations.js';
test('uses only Proved final_me and calculates a meal',()=>{const f={id:1,final_me:1000,'수분':80};assert.equal(resolvedKcal({...f,official_me:9999}),1000);assert.deepEqual(mealNutrition({amount_g:40,added_water_ml:20},f),{grams:40,addedWater:20,kcal:40,foodWater:32})});
test('keeps missing nutrition distinct from zero',()=>{const t=dailyTotals([{feed_id:1,amount_g:20,added_water_ml:5}],[{id:1,final_me:null,'수분':null}]);assert.equal(t.kcal,0);assert.equal(t.foodWater,0);assert.equal(t.addedWater,5);assert.equal(t.missingKcal,true);assert.equal(t.missingMoisture,true)});
test('sums four meals',()=>{const meals=Array.from({length:4},()=>({feed_id:1,amount_g:25,added_water_ml:10}));assert.deepEqual(dailyTotals(meals,[{id:1,final_me:1200,'수분':80}]),{grams:100,kcal:120,foodWater:80,addedWater:40,missingKcal:false,missingMoisture:false})});
test('weekly target never passes goal',()=>{const g={start_date:'2026-01-01',start_weight:4,goal_weight:3.6,weekly_change_kg:-.05};assert.equal(targetForDate(g,'2026-01-15'),3.9);assert.equal(targetForDate(g,'2027-01-01'),3.6)});
